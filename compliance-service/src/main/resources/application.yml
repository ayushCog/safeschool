server:
  port: 8082

spring:
  application:
    name: compliance-service
  datasource:
    url: jdbc:mysql://localhost:3306/safeschool_compliance_db
    username: root
    password: root
  jpa:
    hibernate:
      ddl-auto: update

eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka/